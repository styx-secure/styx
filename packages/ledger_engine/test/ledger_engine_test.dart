import 'package:styx_ledger_engine/styx_ledger_engine.dart';
import 'package:test/test.dart';

void main() {
  test('package name is correct', () {
    expect(packageName(), 'styx_ledger_engine');
  });
}
