import 'dart:typed_data';

/// Constant-time comparison of two [Uint8List]s.
bool uint8ListEquals(Uint8List a, Uint8List b) {
  if (a.length != b.length) return false;
  var result = 0;
  for (var i = 0; i < a.length; i++) {
    result |= a[i] ^ b[i];
  }
  return result == 0;
}
