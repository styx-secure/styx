#!/usr/bin/env python3
import isolated_git
import review_hardening
import security_hardening
import styx_agent

isolated_git.apply(styx_agent)
security_hardening.apply(styx_agent)
review_hardening.apply(security_hardening)
raise SystemExit(styx_agent.main())
