# PWA Fase 1 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rendere Styx Chat una PWA installabile (manifest, icone, service worker con precache offline incl. WASM) e mostrare notifiche locali quando l'app è viva ma non in primo piano.

**Architecture:** L'app React/Vite in `styx-js/apps/chat` acquisisce `vite-plugin-pwa` in strategia `injectManifest`: un service worker nostro (`src/sw.js`) precache-a l'app shell e la WASM di OpenMLS, così apre offline; il SW tiene già lo scheletro degli handler `push`/`notificationclick` che la Fase 2 riempirà. Le notifiche locali sono un modulo puro (`src/lib/notify.js`) agganciato all'evento `onMessage` dell'hook, che notifica solo quando la pagina non è visibile. Un set di icone PNG è generato da un SVG con `sharp`.

**Tech Stack:** Vite 5, `vite-plugin-pwa` (Workbox, injectManifest), React 18, `sharp` (rasterizzazione icone, devDep), Jest (unit, dal root `styx-js`), Playwright (e2e offline via build+preview).

## Global Constraints

- **Runtime target:** Dart SDK non c'entra qui; l'app è JS/Vite. Node per gli script di build.
- **Lingua UI e copy:** italiano. Codice/identificatori/commenti/commit in inglese.
- **Commit:** Conventional Commits, ognuno chiuso da `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- **Branch:** lavorare su `feature/pwa-notifications` (ramificato da `feature/styx-chat-mls`). Non committare su `main`.
- **Notifica generica:** il corpo della notifica è sempre e solo `"Hai un nuovo messaggio"` — nessun contenuto, nessun mittente (vincolo E2E della spec).
- **WASM precache:** `maximumFileSizeToCacheInBytes` ≥ 3_000_000 (la WASM è ~1.8 MB).
- **Test unit dell'app:** vivono in `styx-js/apps/chat/test/**/*.test.js` e girano col Jest del root (`testMatch: ['**/test/**/*.test.js']`) via `node --experimental-vm-modules node_modules/.bin/jest <path> --forceExit` eseguito dalla dir `styx-js`.
- **Working dir dei comandi:** salvo diversa indicazione, i comandi `npm`/`vite`/`playwright` girano da `styx-js/apps/chat`; i comandi `jest` girano da `styx-js`.

---

## File Structure

**Nuovi (in `styx-js/apps/chat/`):**
- `pwa.config.js` — oggetto `manifest` esportato (consumato da `vite.config.js` e dal test).
- `scripts/gen-icons.mjs` — genera i PNG delle icone da SVG inline con `sharp`.
- `public/icon.svg` — logo vettoriale (favicon + sorgente icone).
- `public/pwa-192.png`, `public/pwa-512.png`, `public/pwa-maskable-512.png`, `public/apple-touch-icon.png` — icone generate (binari committati).
- `src/sw.js` — service worker (precache + scheletro push/notificationclick).
- `src/lib/notify.js` — notifier locale puro + helper browser.
- `test/notify.test.js`, `test/pwa-manifest.test.js`, `test/install-hint.test.js` — unit.
- `src/components/InstallHint.jsx` — banner install (Android `beforeinstallprompt`, iOS "Aggiungi a Home").
- `playwright.pwa.config.js`, `e2e/pwa.spec.js` — e2e offline shell (build+preview).

**Modificati:**
- `styx-js/apps/chat/package.json` — devDeps + script.
- `styx-js/apps/chat/vite.config.js` — plugin `VitePWA`.
- `styx-js/apps/chat/index.html` — `apple-touch-icon`, `theme-color`.
- `styx-js/apps/chat/src/hooks/useStyxChat.js` — aggancio notifier su `onMessage`.
- `styx-js/apps/chat/src/components/SettingsPanel.jsx` — sezione "Notifiche" (attiva permesso).
- `styx-js/apps/chat/src/App.jsx` — render di `InstallHint`.

---

## Task 0: Branch di lavoro

- [ ] **Step 1: Creare il branch**

```bash
cd /mnt/storage/home-mverde/src/Styx
git checkout -b feature/pwa-notifications
git branch --show-current   # atteso: feature/pwa-notifications
```

---

## Task 1: Manifest, icone e service worker (PWA installabile)

**Files:**
- Create: `styx-js/apps/chat/pwa.config.js`
- Create: `styx-js/apps/chat/scripts/gen-icons.mjs`
- Create: `styx-js/apps/chat/public/icon.svg`
- Create (generati): `styx-js/apps/chat/public/pwa-192.png`, `pwa-512.png`, `pwa-maskable-512.png`, `apple-touch-icon.png`
- Create: `styx-js/apps/chat/src/sw.js`
- Modify: `styx-js/apps/chat/package.json` (devDeps + script `gen:icons`)
- Modify: `styx-js/apps/chat/vite.config.js` (plugin `VitePWA`)
- Modify: `styx-js/apps/chat/index.html` (`apple-touch-icon`, `theme-color`)
- Test: `styx-js/apps/chat/test/pwa-manifest.test.js`

**Interfaces:**
- Produces: `pwa.config.js` esporta `export const manifest = {...}` con almeno `name`, `short_name`, `start_url`, `display`, `theme_color`, `background_color`, `icons` (array con 192 e 512).
- Produces: `src/sw.js` service worker con `precacheAndRoute(self.__WB_MANIFEST)` e listener `push`/`notificationclick`.

- [ ] **Step 1: Scrivere il test del manifest (fallisce)**

Create `styx-js/apps/chat/test/pwa-manifest.test.js`:

```js
// test/pwa-manifest.test.js — the web app manifest must carry the fields a
// browser needs to consider the app installable.
import { describe, test, expect } from '@jest/globals';
import { manifest } from '../pwa.config.js';

describe('PWA manifest', () => {
  test('has the required installability fields', () => {
    expect(manifest.name).toBeTruthy();
    expect(manifest.short_name).toBeTruthy();
    expect(manifest.start_url).toBe('/');
    expect(manifest.display).toBe('standalone');
    expect(manifest.theme_color).toMatch(/^#/);
    expect(manifest.background_color).toMatch(/^#/);
  });

  test('declares 192px and 512px png icons plus a maskable one', () => {
    const sizes = manifest.icons.map((i) => i.sizes);
    expect(sizes).toContain('192x192');
    expect(sizes).toContain('512x512');
    expect(manifest.icons.some((i) => i.purpose === 'maskable')).toBe(true);
    expect(manifest.icons.every((i) => i.type === 'image/png')).toBe(true);
  });
});
```

- [ ] **Step 2: Eseguire il test → deve fallire**

Run (da `styx-js`):
```bash
node --experimental-vm-modules node_modules/.bin/jest apps/chat/test/pwa-manifest.test.js --forceExit
```
Expected: FAIL — `Cannot find module '../pwa.config.js'`.

- [ ] **Step 3: Creare `pwa.config.js`**

Create `styx-js/apps/chat/pwa.config.js`:

```js
// pwa.config.js — the web app manifest, shared by vite.config.js (build) and
// the unit test. Kept free of vite imports so Jest can import it directly.
export const manifest = {
  name: 'Styx Chat',
  short_name: 'Styx',
  description: 'Messaggistica sovrana, end-to-end e serverless.',
  id: '/',
  start_url: '/',
  scope: '/',
  display: 'standalone',
  orientation: 'portrait',
  lang: 'it',
  theme_color: '#0d9f6e',
  background_color: '#04070a',
  icons: [
    { src: 'pwa-192.png', sizes: '192x192', type: 'image/png' },
    { src: 'pwa-512.png', sizes: '512x512', type: 'image/png' },
    { src: 'pwa-maskable-512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
  ],
};
```

- [ ] **Step 4: Eseguire il test → deve passare**

Run (da `styx-js`):
```bash
node --experimental-vm-modules node_modules/.bin/jest apps/chat/test/pwa-manifest.test.js --forceExit
```
Expected: PASS (2 test verdi).

- [ ] **Step 5: Creare il logo SVG**

Create `styx-js/apps/chat/public/icon.svg`:

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="512" height="512">
  <defs>
    <linearGradient id="g" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0" stop-color="#18b981"/>
      <stop offset="1" stop-color="#0b8a60"/>
    </linearGradient>
  </defs>
  <rect width="512" height="512" rx="112" fill="url(#g)"/>
  <path d="M150 176c60-40 152-40 212 0" fill="none" stroke="#ffffff" stroke-width="30" stroke-linecap="round"/>
  <path d="M150 256c60 40 152 40 212 0" fill="none" stroke="#ffffff" stroke-width="30" stroke-linecap="round"/>
  <path d="M150 336c60-40 152-40 212 0" fill="none" stroke="#ffffff" stroke-width="30" stroke-linecap="round"/>
</svg>
```

- [ ] **Step 6: Installare le devDeps e scrivere lo script di generazione icone**

Run (da `styx-js/apps/chat`):
```bash
npm install -D vite-plugin-pwa sharp
```
Expected: installazione ok (aggiunge `vite-plugin-pwa` e `sharp` a devDependencies).

Create `styx-js/apps/chat/scripts/gen-icons.mjs`:

```js
// scripts/gen-icons.mjs — rasterize the app logo into the PNG icons the manifest
// and iOS home screen need. Run once (npm run gen:icons); PNGs are committed.
import sharp from 'sharp';
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';

const pub = (p) => fileURLToPath(new URL(`../public/${p}`, import.meta.url));
const svg = readFileSync(pub('icon.svg'));

// Maskable icon: same art on a full-bleed square (no rounded corners) so the
// platform mask can apply its own shape without clipping the glyph.
const maskableSvg = Buffer.from(`
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="512" height="512">
  <rect width="512" height="512" fill="#0b8a60"/>
  <path d="M170 190c52-34 130-34 182 0" fill="none" stroke="#ffffff" stroke-width="26" stroke-linecap="round"/>
  <path d="M170 256c52 34 130 34 182 0" fill="none" stroke="#ffffff" stroke-width="26" stroke-linecap="round"/>
  <path d="M170 322c52-34 130-34 182 0" fill="none" stroke="#ffffff" stroke-width="26" stroke-linecap="round"/>
</svg>`);

async function main() {
  await sharp(svg).resize(192, 192).png().toFile(pub('pwa-192.png'));
  await sharp(svg).resize(512, 512).png().toFile(pub('pwa-512.png'));
  await sharp(svg).resize(180, 180).png().toFile(pub('apple-touch-icon.png'));
  await sharp(maskableSvg).resize(512, 512).png().toFile(pub('pwa-maskable-512.png'));
  console.log('icons written to public/');
}
main().catch((e) => { console.error(e); process.exit(1); });
```

Add the script to `styx-js/apps/chat/package.json` `"scripts"` (insert after `"preview"`):

```json
    "gen:icons": "node scripts/gen-icons.mjs",
```

- [ ] **Step 7: Generare le icone**

Run (da `styx-js/apps/chat`):
```bash
npm run gen:icons
ls -la public/*.png
```
Expected: quattro PNG in `public/` (`pwa-192.png`, `pwa-512.png`, `pwa-maskable-512.png`, `apple-touch-icon.png`).

- [ ] **Step 8: Creare il service worker**

Create `styx-js/apps/chat/src/sw.js`:

```js
// sw.js — Styx Chat service worker (vite-plugin-pwa injectManifest strategy).
// Precaches the app shell + OpenMLS WASM so the app opens offline. The push /
// notificationclick listeners are the stable skeleton Phase 2 (Web Push) fills in.
import { precacheAndRoute } from 'workbox-precaching';

precacheAndRoute(self.__WB_MANIFEST);

self.addEventListener('install', () => self.skipWaiting());
self.addEventListener('activate', (event) => event.waitUntil(self.clients.claim()));

// Phase 2 fills this in: show a generic notification on a Web Push. No-op for now.
self.addEventListener('push', () => {});

self.addEventListener('notificationclick', (event) => {
  event.notification.close();
  event.waitUntil((async () => {
    const clients = await self.clients.matchAll({ type: 'window', includeUncontrolled: true });
    if (clients.length) return clients[0].focus();
    return self.clients.openWindow('/');
  })());
});
```

- [ ] **Step 9: Collegare `vite-plugin-pwa` nella config di Vite**

Modify `styx-js/apps/chat/vite.config.js` — add the import and the plugin. Full file:

```js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { VitePWA } from 'vite-plugin-pwa';
import { fileURLToPath } from 'node:url';
import { manifest } from './pwa.config.js';

// Resolve `styx-js` to the library source in the parent package, so the app
// consumes the real StyxChat (MLS/OpenMLS) instead of the mock.
const styxJsEntry = fileURLToPath(new URL('../../src/index.js', import.meta.url));
const styxJsRoot = fileURLToPath(new URL('../../', import.meta.url));

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      strategies: 'injectManifest',
      srcDir: 'src',
      filename: 'sw.js',
      registerType: 'autoUpdate',
      injectRegister: 'auto',
      includeAssets: ['apple-touch-icon.png', 'icon.svg'],
      manifest,
      injectManifest: {
        // The OpenMLS WASM is ~1.8 MB — raise the precache size ceiling.
        maximumFileSizeToCacheInBytes: 3_000_000,
        globPatterns: ['**/*.{js,css,html,wasm,png,svg,woff2}'],
      },
      // Keep the SW out of the dev server so the existing dev-based e2e is
      // unaffected; the PWA e2e runs against a production build + preview.
      devOptions: { enabled: false },
    }),
  ],
  resolve: {
    alias: { 'styx-js': styxJsEntry },
  },
  server: {
    port: 5175,
    fs: { allow: [styxJsRoot] },
  },
  preview: {
    port: 8090,
    host: '127.0.0.1',
    allowedHosts: true,
  },
  assetsInclude: ['**/*.wasm'],
  optimizeDeps: {
    exclude: ['styx-js'],
  },
});
```

- [ ] **Step 10: Aggiungere apple-touch-icon e theme-color all'index.html**

Modify `styx-js/apps/chat/index.html` — add three lines inside `<head>` after the `<meta name="description" ...>` line:

```html
    <meta name="theme-color" content="#0d9f6e" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="icon" type="image/svg+xml" href="/icon.svg" />
```

- [ ] **Step 11: Build → verifica che il SW e il manifest siano generati**

Run (da `styx-js/apps/chat`):
```bash
npm run build
ls -la dist/sw.js dist/manifest.webmanifest dist/pwa-512.png
```
Expected: build ok; esistono `dist/sw.js`, `dist/manifest.webmanifest`, `dist/pwa-512.png`. Il SW contiene il precache manifest (grep di controllo):
```bash
grep -c "precache" dist/sw.js   # atteso: > 0
```

- [ ] **Step 12: Rieseguire tutti i test dell'app + build come regressione**

Run (da `styx-js`):
```bash
node --experimental-vm-modules node_modules/.bin/jest apps/chat/test/pwa-manifest.test.js --forceExit
```
Expected: PASS.

- [ ] **Step 13: Commit**

```bash
cd /mnt/storage/home-mverde/src/Styx/styx-js/apps/chat
git add package.json package-lock.json pwa.config.js vite.config.js index.html \
  scripts/gen-icons.mjs src/sw.js public/icon.svg public/pwa-192.png \
  public/pwa-512.png public/pwa-maskable-512.png public/apple-touch-icon.png
cd /mnt/storage/home-mverde/src/Styx
git add styx-js/apps/chat/test/pwa-manifest.test.js
git commit -m "feat(chat): installable PWA — manifest, icons, precaching service worker

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: E2E offline shell (build + preview)

**Files:**
- Create: `styx-js/apps/chat/playwright.pwa.config.js`
- Create: `styx-js/apps/chat/e2e/pwa.spec.js`
- Modify: `styx-js/apps/chat/package.json` (script `test:e2e:pwa`)

**Interfaces:**
- Consumes: la build di Task 1 (SW `dist/sw.js` + precache).

- [ ] **Step 1: Aggiungere lo script e2e**

Add to `styx-js/apps/chat/package.json` `"scripts"` (after `"test:e2e"`):

```json
    "test:e2e:pwa": "playwright test -c playwright.pwa.config.js",
```

- [ ] **Step 2: Config Playwright dedicata (build + preview)**

Create `styx-js/apps/chat/playwright.pwa.config.js`:

```js
import { defineConfig } from '@playwright/test';

// PWA e2e needs the real service worker, which vite-plugin-pwa only emits in a
// production build. So this config builds then serves `dist/` via vite preview.
export default defineConfig({
  testDir: './e2e',
  testMatch: 'pwa.spec.js',
  timeout: 120_000,
  workers: 1,
  use: {
    baseURL: 'http://127.0.0.1:4180',
    headless: true,
    ...(process.env.PW_EXECUTABLE
      ? { launchOptions: { executablePath: process.env.PW_EXECUTABLE } }
      : {}),
  },
  webServer: {
    command: 'npm run build && npm run preview -- --port 4180 --host 127.0.0.1',
    url: 'http://127.0.0.1:4180',
    reuseExistingServer: false,
    timeout: 120_000,
  },
});
```

- [ ] **Step 3: Scrivere lo spec offline (fallisce senza SW attivo)**

Create `styx-js/apps/chat/e2e/pwa.spec.js`:

```js
import { test, expect } from '@playwright/test';

// The app must register a service worker, precache its shell, and still render
// the unlock screen after the network is cut and the page reloaded.
test('registers a service worker and loads offline', async ({ page, context }) => {
  await page.goto('/');

  // Wait until the service worker controls the page (precache done).
  await page.waitForFunction(async () => {
    if (!('serviceWorker' in navigator)) return false;
    const reg = await navigator.serviceWorker.getRegistration();
    return !!(reg && (reg.active || reg.installing || reg.waiting));
  }, null, { timeout: 60_000 });
  await page.waitForFunction(() => navigator.serviceWorker.controller !== null, null, { timeout: 60_000 });

  // Cut the network and reload: the shell must still come from cache.
  await context.setOffline(true);
  await page.reload();

  // The unlock screen (password field) is the app shell entry point.
  await expect(page.locator('input[type="password"]')).toBeVisible({ timeout: 30_000 });

  await context.setOffline(false);
});
```

- [ ] **Step 4: Eseguire l'e2e → deve passare**

Run (da `styx-js/apps/chat`):
```bash
PW_EXECUTABLE="$(ls ~/.cache/ms-playwright/chromium-*/chrome-linux/chrome 2>/dev/null | head -1)" npm run test:e2e:pwa
```
Expected: 1 test PASS. (`input[type="password"]` è il campo password della `UnlockScreen`, verificato presente — è l'elemento dell'app shell atteso offline.)

- [ ] **Step 5: Commit**

```bash
cd /mnt/storage/home-mverde/src/Styx
git add styx-js/apps/chat/playwright.pwa.config.js styx-js/apps/chat/e2e/pwa.spec.js styx-js/apps/chat/package.json
git commit -m "test(chat): e2e for PWA offline app-shell load

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: Modulo notifiche locali + aggancio all'hook

**Files:**
- Create: `styx-js/apps/chat/src/lib/notify.js`
- Test: `styx-js/apps/chat/test/notify.test.js`
- Modify: `styx-js/apps/chat/src/hooks/useStyxChat.js`

**Interfaces:**
- Produces: `createNotifier({ getPermission, isPageVisible, show, now, coalesceMs }) -> { notifyIncoming(): boolean }`.
- Produces: `browserNotifier() -> notifier` (reali globali del browser).
- Produces: `requestNotificationPermission() -> Promise<'granted'|'denied'|'default'>`.
- Consumes (in `useStyxChat`): `chat.onMessage(cb)` con `msg.direction === 'in'` per i messaggi in arrivo.

- [ ] **Step 1: Scrivere i test del notifier (falliscono)**

Create `styx-js/apps/chat/test/notify.test.js`:

```js
// test/notify.test.js — the local notifier decides whether to raise a browser
// notification, using injected deps so it is testable without a DOM.
import { describe, test, expect, jest } from '@jest/globals';
import { createNotifier } from '../src/lib/notify.js';

function harness({ permission = 'granted', visible = false, t = 1000 } = {}) {
  const shown = [];
  let now = t;
  const notifier = createNotifier({
    getPermission: () => permission,
    isPageVisible: () => visible,
    show: (n) => shown.push(n),
    now: () => now,
    coalesceMs: 4000,
  });
  return { notifier, shown, advance: (ms) => { now += ms; } };
}

describe('createNotifier', () => {
  test('shows a generic notification when hidden and permission granted', () => {
    const { notifier, shown } = harness({ visible: false, permission: 'granted' });
    expect(notifier.notifyIncoming()).toBe(true);
    expect(shown).toHaveLength(1);
    expect(shown[0].body).toBe('Hai un nuovo messaggio');
    expect(shown[0].title).toBe('Styx Chat');
  });

  test('stays silent when the page is visible (foreground)', () => {
    const { notifier, shown } = harness({ visible: true });
    expect(notifier.notifyIncoming()).toBe(false);
    expect(shown).toHaveLength(0);
  });

  test('stays silent when permission is not granted', () => {
    const { notifier, shown } = harness({ permission: 'default' });
    expect(notifier.notifyIncoming()).toBe(false);
    expect(shown).toHaveLength(0);
  });

  test('coalesces a burst into one notification within the window', () => {
    const { notifier, shown, advance } = harness({ visible: false });
    expect(notifier.notifyIncoming()).toBe(true);
    advance(1000);
    expect(notifier.notifyIncoming()).toBe(false); // within 4s window
    advance(4000);
    expect(notifier.notifyIncoming()).toBe(true);  // window elapsed
    expect(shown).toHaveLength(2);
  });
});
```

- [ ] **Step 2: Eseguire i test → devono fallire**

Run (da `styx-js`):
```bash
node --experimental-vm-modules node_modules/.bin/jest apps/chat/test/notify.test.js --forceExit
```
Expected: FAIL — `Cannot find module '../src/lib/notify.js'`.

- [ ] **Step 3: Implementare `notify.js`**

Create `styx-js/apps/chat/src/lib/notify.js`:

```js
// notify.js — local (in-app) notifications. The pure `createNotifier` holds the
// decision logic (permission × page visibility × coalescing) with injected deps
// so it is unit-testable; `browserNotifier` wires the real browser globals.

const TITLE = 'Styx Chat';
const BODY = 'Hai un nuovo messaggio'; // generic on purpose — content stays E2E
const TAG = 'styx-new';

/**
 * @param {object} deps
 * @param {() => string} deps.getPermission current Notification permission
 * @param {() => boolean} deps.isPageVisible whether the app is in the foreground
 * @param {(n:{title:string,body:string,tag:string}) => void} deps.show raise it
 * @param {() => number} deps.now epoch millis
 * @param {number} [deps.coalesceMs] min gap between notifications
 */
export function createNotifier({ getPermission, isPageVisible, show, now, coalesceMs = 4000 }) {
  let lastShownAt = -Infinity;
  return {
    /** Decide + maybe show a notification for an inbound event. @returns {boolean} shown */
    notifyIncoming() {
      if (getPermission() !== 'granted') return false;
      if (isPageVisible()) return false; // foreground: the UI already shows it
      const t = now();
      if (t - lastShownAt < coalesceMs) return false; // coalesce bursts
      lastShownAt = t;
      show({ title: TITLE, body: BODY, tag: TAG });
      return true;
    },
  };
}

/** Notifier wired to the real browser globals. Safe no-op notifier if unsupported. */
export function browserNotifier() {
  const supported = typeof Notification !== 'undefined' && typeof document !== 'undefined';
  if (!supported) return { notifyIncoming: () => false };
  return createNotifier({
    getPermission: () => Notification.permission,
    isPageVisible: () => document.visibilityState === 'visible',
    show: ({ title, body, tag }) => { try { new Notification(title, { body, tag }); } catch { /* ignore */ } },
    now: () => Date.now(),
  });
}

/** Ask for notification permission (call on a user gesture). @returns {Promise<string>} */
export async function requestNotificationPermission() {
  if (typeof Notification === 'undefined') return 'denied';
  try { return await Notification.requestPermission(); } catch { return 'denied'; }
}
```

- [ ] **Step 4: Eseguire i test → devono passare**

Run (da `styx-js`):
```bash
node --experimental-vm-modules node_modules/.bin/jest apps/chat/test/notify.test.js --forceExit
```
Expected: PASS (4 test verdi).

- [ ] **Step 5: Agganciare il notifier all'hook**

Modify `styx-js/apps/chat/src/hooks/useStyxChat.js`.

(a) Add the import after the existing imports (after the `getRelays` import, line 8):

```js
import { browserNotifier } from '../lib/notify.js';
```

(b) Add a ref alongside the other refs (after `const typingTimers = useRef({});`, line 15):

```js
  const notifierRef = useRef(null);
  if (!notifierRef.current) notifierRef.current = browserNotifier();
```

(c) In the `chat.onMessage` subscription (currently `chat.onMessage((msg) => upsertMessage(msg))`, line 70), raise a local notification for inbound messages:

```js
      chat.onMessage((msg) => {
        upsertMessage(msg);
        if (msg.direction === 'in') notifierRef.current.notifyIncoming();
      }),
```

- [ ] **Step 6: Verifica di non-regressione (l'app compila e i test unit passano)**

Run (da `styx-js/apps/chat`):
```bash
npm run build
```
Expected: build ok.

Run (da `styx-js`):
```bash
node --experimental-vm-modules node_modules/.bin/jest apps/chat/test/notify.test.js apps/chat/test/pwa-manifest.test.js --forceExit
```
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
cd /mnt/storage/home-mverde/src/Styx
git add styx-js/apps/chat/src/lib/notify.js styx-js/apps/chat/test/notify.test.js styx-js/apps/chat/src/hooks/useStyxChat.js
git commit -m "feat(chat): local notifications for inbound messages when backgrounded

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: UX permesso notifiche + hint di installazione

**Files:**
- Create: `styx-js/apps/chat/src/lib/install-hint.js` (logica pura)
- Create: `styx-js/apps/chat/src/components/InstallHint.jsx` (componente)
- Test: `styx-js/apps/chat/test/install-hint.test.js`
- Modify: `styx-js/apps/chat/src/components/SettingsPanel.jsx`
- Modify: `styx-js/apps/chat/src/App.jsx`

**Interfaces:**
- Consumes: `requestNotificationPermission()` da `src/lib/notify.js`.
- Produces: `installHintKind({ standalone, isIOS, deferredPrompt }) -> 'none'|'ios'|'android'` (funzione pura, esportata da `src/lib/install-hint.js` — nessun import di React, così è Jest-importabile con `transform: {}`).

- [ ] **Step 1: Scrivere il test della logica hint (fallisce)**

Create `styx-js/apps/chat/test/install-hint.test.js`:

```js
// test/install-hint.test.js — which install hint to show, if any. Pure logic so
// it is testable without a DOM (and without importing React under Jest).
import { describe, test, expect } from '@jest/globals';
import { installHintKind } from '../src/lib/install-hint.js';

describe('installHintKind', () => {
  test('no hint when already installed (standalone)', () => {
    expect(installHintKind({ standalone: true, isIOS: true, deferredPrompt: null })).toBe('none');
    expect(installHintKind({ standalone: true, isIOS: false, deferredPrompt: {} })).toBe('none');
  });

  test('iOS Safari (not standalone) gets the add-to-home hint', () => {
    expect(installHintKind({ standalone: false, isIOS: true, deferredPrompt: null })).toBe('ios');
  });

  test('Android with a captured install prompt gets the install button', () => {
    expect(installHintKind({ standalone: false, isIOS: false, deferredPrompt: {} })).toBe('android');
  });

  test('non-iOS without a captured prompt shows nothing', () => {
    expect(installHintKind({ standalone: false, isIOS: false, deferredPrompt: null })).toBe('none');
  });
});
```

- [ ] **Step 2: Eseguire il test → deve fallire**

Run (da `styx-js`):
```bash
node --experimental-vm-modules node_modules/.bin/jest apps/chat/test/install-hint.test.js --forceExit
```
Expected: FAIL — `Cannot find module '../src/lib/install-hint.js'`.

- [ ] **Step 3a: Implementare la logica pura `src/lib/install-hint.js`**

Create `styx-js/apps/chat/src/lib/install-hint.js`:

```js
// install-hint.js — pure decision for which install hint to show. No React,
// no DOM: unit-testable under Jest and imported by the InstallHint component.

/** @returns {'none'|'ios'|'android'} */
export function installHintKind({ standalone, isIOS, deferredPrompt }) {
  if (standalone) return 'none';
  if (isIOS) return 'ios';
  if (deferredPrompt) return 'android';
  return 'none';
}

export function isIOSDevice() {
  if (typeof navigator === 'undefined') return false;
  return /iphone|ipad|ipod/i.test(navigator.userAgent);
}

export function isStandalone() {
  if (typeof window === 'undefined') return false;
  return window.matchMedia?.('(display-mode: standalone)').matches
    || window.navigator.standalone === true;
}
```

- [ ] **Step 3b: Implementare il componente `InstallHint.jsx`**

Create `styx-js/apps/chat/src/components/InstallHint.jsx`:

```jsx
import { useEffect, useState } from 'react';
import { installHintKind, isIOSDevice, isStandalone } from '../lib/install-hint.js';

export default function InstallHint() {
  const [deferred, setDeferred] = useState(null);
  const [dismissed, setDismissed] = useState(() => {
    try { return localStorage.getItem('styx-install-dismissed') === '1'; } catch { return false; }
  });

  useEffect(() => {
    const onPrompt = (e) => { e.preventDefault(); setDeferred(e); };
    window.addEventListener('beforeinstallprompt', onPrompt);
    return () => window.removeEventListener('beforeinstallprompt', onPrompt);
  }, []);

  if (dismissed) return null;
  const kind = installHintKind({ standalone: isStandalone(), isIOS: isIOSDevice(), deferredPrompt: deferred });
  if (kind === 'none') return null;

  const dismiss = () => {
    setDismissed(true);
    try { localStorage.setItem('styx-install-dismissed', '1'); } catch { /* ignore */ }
  };

  const install = async () => {
    if (!deferred) return;
    deferred.prompt();
    try { await deferred.userChoice; } catch { /* ignore */ }
    setDeferred(null);
    dismiss();
  };

  const text = kind === 'ios'
    ? 'Installa Styx: tocca Condividi e poi "Aggiungi a Home" per abilitare le notifiche.'
    : 'Installa Styx come app per riceverne le notifiche.';

  return (
    <div className="install-hint sx-badge">
      <span style={{ flex: 1 }}>{text}</span>
      {kind === 'android' && (
        <button className="btn btn-accent" style={{ padding: '0 12px' }} onClick={install}>Installa</button>
      )}
      <button className="icon-btn" onClick={dismiss} aria-label="Chiudi">×</button>
    </div>
  );
}
```

- [ ] **Step 4: Eseguire il test → deve passare**

Run (da `styx-js`):
```bash
node --experimental-vm-modules node_modules/.bin/jest apps/chat/test/install-hint.test.js --forceExit
```
Expected: PASS (4 test verdi).

- [ ] **Step 5: Aggiungere lo stile del banner**

Add to `styx-js/apps/chat/src/styles/app.css` (append at end of file):

```css
/* ============ InstallHint ============ */
.install-hint { position: fixed; left: 12px; right: 12px; bottom: 12px; z-index: 50;
  display: flex; align-items: center; gap: 10px; padding: 10px 12px; border-radius: 14px;
  background: var(--panel); border: 1px solid var(--border-2); box-shadow: var(--shadow-lg);
  font-size: 13px; }
```

- [ ] **Step 6: Renderizzare `InstallHint` in App.jsx**

Modify `styx-js/apps/chat/src/App.jsx`:

(a) Add the import after the `SettingsPanel` import (line 6):

```js
import InstallHint from './components/InstallHint.jsx';
```

(b) Render it inside the top-level fragment — add just before the closing `</>` (after the `{toast && ...}` line, line 121):

```jsx
      <InstallHint />
```

- [ ] **Step 7: Aggiungere la sezione "Notifiche" nelle Impostazioni**

Modify `styx-js/apps/chat/src/components/SettingsPanel.jsx`:

(a) Add the import after the existing imports (line 3):

```js
import { requestNotificationPermission } from '../lib/notify.js';
```

(b) Add permission state after the `alias` state (line 6):

```js
  const [notifPerm, setNotifPerm] = useState(
    typeof Notification !== 'undefined' ? Notification.permission : 'denied',
  );
```

(c) Add an "enable notifications" handler after `copyKey` (after line 13):

```js
  const enableNotifications = async () => {
    const p = await requestNotificationPermission();
    setNotifPerm(p);
    onToast?.(p === 'granted' ? 'Notifiche attive ✓' : 'Notifiche non attive');
  };
```

(d) Add a "Notifiche" section — insert right after the security-card `</div>` section (after line 48, before the contacts section):

```jsx
        <div className="section">
          <label className="label">Notifiche</label>
          <div style={{ marginTop: 6 }}>
            {notifPerm === 'granted' && <div style={{ fontSize: 13, color: 'var(--accent-2)' }}>Attive ✓</div>}
            {notifPerm === 'denied' && <div style={{ fontSize: 13, color: 'var(--text-dim)' }}>Bloccate dal browser — riattivale dalle impostazioni del sito.</div>}
            {notifPerm === 'default' && (
              <button className="btn btn-accent" onClick={enableNotifications}>Attiva notifiche</button>
            )}
          </div>
        </div>
```

- [ ] **Step 8: Verifica build + tutti i test unit dell'app**

Run (da `styx-js/apps/chat`):
```bash
npm run build
```
Expected: build ok.

Run (da `styx-js`):
```bash
node --experimental-vm-modules node_modules/.bin/jest apps/chat/test --forceExit
```
Expected: PASS (notify + pwa-manifest + install-hint, tutti verdi).

- [ ] **Step 9: Commit**

```bash
cd /mnt/storage/home-mverde/src/Styx
git add styx-js/apps/chat/src/lib/install-hint.js styx-js/apps/chat/src/components/InstallHint.jsx \
  styx-js/apps/chat/test/install-hint.test.js styx-js/apps/chat/src/components/SettingsPanel.jsx \
  styx-js/apps/chat/src/App.jsx styx-js/apps/chat/src/styles/app.css
git commit -m "feat(chat): notification-permission UX + install hint (iOS/Android)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Verifica finale (Fase 1)

- [ ] **Unit (dal root `styx-js`):**
```bash
node --experimental-vm-modules node_modules/.bin/jest apps/chat/test --forceExit
```
Tutti verdi (manifest, notify, install-hint).

- [ ] **E2E offline (da `styx-js/apps/chat`):**
```bash
PW_EXECUTABLE="$(ls ~/.cache/ms-playwright/chromium-*/chrome-linux/chrome 2>/dev/null | head -1)" npm run test:e2e:pwa
```
1 test PASS.

- [ ] **Non-regressione e2e pairing (da `styx-js/apps/chat`):**
```bash
PW_EXECUTABLE="$(ls ~/.cache/ms-playwright/chromium-*/chrome-linux/chrome 2>/dev/null | head -1)" npm run test:e2e
```
Il test di pairing esistente resta verde (il SW è disattivato in dev).

- [ ] **Manuale (dispositivo reale, dietro il tunnel):** installare la PWA su iPhone/Android da `https://styxmessenger.maurizioverde.info/` (build servita da `dist/`), verificare icona in home + avvio standalone; con l'app in background e permesso concesso, un messaggio in arrivo mostra "Hai un nuovo messaggio". Nota: dopo il deploy assicurarsi che il server statico serva `dist/sw.js` e `dist/manifest.webmanifest`.

---

## Note per la Fase 2 (fuori da questo piano)

Lo scheletro `push`/`notificationclick` in `src/sw.js` e il `notificationclick` sono già pronti: la Fase 2 aggiungerà il `PushRegistrar` lato client, il bridge Node + Web Push, e riempirà l'handler `push` con `self.registration.showNotification(...)`.
